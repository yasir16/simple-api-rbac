# simple-api-rbac

# How To run
```docker-compose up --wait```
```docker-compose up ```

# Available End Point

for post, put , delete need auth

```[GET] /books```
    
```[GET] /books/{id} ```

```[POST] /books```

```[PUT] /books/{id}```

```[DELETE] /books/{id}```

